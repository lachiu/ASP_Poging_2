@model VoorraadSysteem.ViewModels.EditUserViewModel

@{
	ViewData["Title"] = "Ticket aanpassen";
}

<h1>Aanpassen</h1>
<h4>kastick</h4>
<hr />
<div class="row">
	<div class="col-md-4">
		<form asp-action="Edit">
			<div asp-validation-summary="ModelOnly" class="text-danger"></div>
			<div class="form-group">
				<input type="hidden" asp-for="Id" />
			</div>
			@*<div class="form-group">
				<input type="hidden" asp-for="created" />
			</div>
			<div class="form-group">
				<label asp-for="name" class="control-label">Naam</label>
				<input asp-for="name" class="form-control" />
				<span asp-validation-for="name" class="text-danger"></span>
			</div>
			<div class="form-group">
				<label asp-for="password" class="control-label">Wachtwoord</label>
				<input asp-for="password" class="form-control" />
				<span asp-validation-for="password" class="text-danger"></span>
			</div>
			<div class="form-group">
				<label asp-for="permission_id" class="control-label">Permissie</label>
				<select asp-for="permission_id" class="form-control" id="permission.id">
					@foreach (var permission in Model.Permissions)
					{
							<option value="@permission.id">@permission.name</option>
					}
				</select>
				<span asp-validation-for="permission_id" class="text-danger"></span>
			</div>*@
			<div class="form-group">
				<input type="submit" value="Save" class="btn btn-primary" />
			</div>
		</form>
	</div>
</div>

<div>
	<a asp-action="Index">Ga terug naar de lijst</a>
</div>

@section Scripts {
	@{
		await Html.RenderPartialAsync("_ValidationScriptsPartial");
	}
}